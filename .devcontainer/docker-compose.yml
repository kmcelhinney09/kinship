services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/kinship:cached
    command: sleep infinity
    ports:
      - "8000:8000"   # backend
      - "5173:5173"   # vite dev server
